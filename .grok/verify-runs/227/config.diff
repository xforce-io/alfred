40a41,43
>       # #227: sidecar only sees SIDECAR_BASE_ENV + framework vars + these.
>       # web(TAVILY) / invest(TUSHARE, FRED) / kairo CLI(KAIRO_PROVIDER=grok, XAI fallback)
>       env_passthrough: [TAVILY_API_KEY, TUSHARE_TOKEN, FRED_API_KEY, KAIRO_PROVIDER, XAI_API_KEY]
48a52
>     # #227: deny by default — only these chat ids may talk to the bots.
51a56
>       allowed_chat_ids: ["8576399597"]
54a60,63
>       allowed_chat_ids: ["8576399597"]
>   web:
>     # #227: required; the web server refuses to start without it.
>     api_key: "${EVERBOT_WEB_API_KEY}"
